{
    "name": "stravapower-root",
    "private": true,
    "version": "1.2.0",
    "scripts": {
        "install:web": "cd web && npm install",
        "build": "export VITE_GIT_HASH=$(git rev-parse --short HEAD) && cd web && npm install && npm run build",
        "preview": "cd web && npm run preview"
    }
}